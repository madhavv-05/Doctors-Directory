# Core Django + DRF
Django>=4.2
djangorestframework>=3.14

# JWT Authentication
djangorestframework-simplejwt>=5.3.0

# PostgreSQL support
psycopg2-binary>=2.9.9

# CORS headers (for frontend-backend communication)
django-cors-headers>=4.3.1

# Google OAuth token verification
google-auth>=2.29.0

# AWS SDK (for SNS if you're using it)
boto3>=1.34.101

# Image uploads (for profile pictures)
Pillow>=10.3.0

# Environment variables (if you're using .env)
python-decouple>=3.8

# Optional but recommended for email validation
# validate-email-address>=1.1.6

# For timezone-aware datetime operations
pytz>=2024.1
